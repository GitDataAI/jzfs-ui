version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run: echo "Running tests"
      - run: npm install
      - run: npm test
workflows:
  version: 2
  build-deploy:
    jobs:
      - build
